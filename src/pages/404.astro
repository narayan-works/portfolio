---
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>404 - Page Not Found | Narayan Ashanahalli</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="error-page">
            <div class="error-content">
                <div class="error-code">
                    <span class="digit">4</span>
                    <div class="ghost-container">
                        <img
                            src="/logos/dead 1.png"
                            alt="Ghost"
                            class="ghost-icon ghost-1"
                        />
                        <img
                            src="/logos/dead 2.png"
                            alt="Ghost"
                            class="ghost-icon ghost-2"
                        />
                        <img
                            src="/logos/dead 3.png"
                            alt="Ghost"
                            class="ghost-icon ghost-3"
                        />
                    </div>
                    <span class="digit">4</span>
                </div>
                <p class="error-title">not.found</p>
                <div class="description-box">
                    <div class="corner-square top-left"></div>
                    <div class="corner-square top-right"></div>
                    <div class="corner-square bottom-left"></div>
                    <div class="corner-square bottom-right"></div>
                    <p class="error-description">
                        The page you are looking for doesn't exist or an other
                        error occurred. Go back, or head over to <a href="/"
                            >narayan.works</a
                        > to choose a new direction.
                    </p>
                </div>
            </div>
        </div>
    </body>
</html>

<style>
    body {
        margin: 0;
        padding: 0;
    }

    .error-page {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-color: var(--accent-secondary, #42bd49);
        padding: 2rem;
    }

    .error-content {
        text-align: center;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .error-code {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
    }

    .digit {
        font-family: "Space Mono", monospace;
        font-size: clamp(6rem, 20vw, 12rem);
        font-weight: 400;
        line-height: 1;
        color: white;
    }

    .ghost-container {
        position: relative;
        width: clamp(80px, 15vw, 140px);
        height: clamp(80px, 15vw, 140px);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .ghost-icon {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 150%;
        height: auto;
        filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
        opacity: 0;
    }

    .ghost-1 {
        animation: frame1 1.5s steps(1) infinite;
    }
    .ghost-2 {
        animation: frame2 1.5s steps(1) infinite;
    }
    .ghost-3 {
        animation: frame3 1.5s steps(1) infinite;
    }

    @keyframes frame1 {
        0%,
        33.33% {
            opacity: 1;
        }
        33.34%,
        100% {
            opacity: 0;
        }
    }

    @keyframes frame2 {
        0%,
        33.33% {
            opacity: 0;
        }
        33.34%,
        66.66% {
            opacity: 1;
        }
        66.67%,
        100% {
            opacity: 0;
        }
    }

    @keyframes frame3 {
        0%,
        66.66% {
            opacity: 0;
        }
        66.67%,
        100% {
            opacity: 1;
        }
    }

    .error-title {
        font-family: "Space Mono", monospace;
        font-size: 1.5rem;
        font-weight: 400;
        color: white;
        margin: 0 0 2rem 0;
    }

    .description-box {
        position: relative;
        background: white;
        border: 2px solid #308ce9;
        border-radius: 4px;
        padding: 1rem 1.5rem;
        max-width: 500px;
    }

    .corner-square {
        position: absolute;
        width: 12px;
        height: 12px;
        background: white;
        border: 2px solid #308ce9;
    }

    .corner-square.top-left {
        top: -7px;
        left: -7px;
    }

    .corner-square.top-right {
        top: -7px;
        right: -7px;
    }

    .corner-square.bottom-left {
        bottom: -7px;
        left: -7px;
    }

    .corner-square.bottom-right {
        bottom: -7px;
        right: -7px;
    }

    .error-description {
        font-family: "Geist", sans-serif;
        font-size: 0.7rem;
        line-height: 1.3;
        color: #333;
        margin: 0;
        text-align: left;
    }

    .error-description a {
        color: #308ce9;
        text-decoration: underline;
        font-weight: 500;
    }

    .error-description a:hover {
        text-decoration: none;
    }

    /* Mobile responsive - vertical stacking */
    @media (max-width: 768px) {
        .error-page {
            flex-direction: column;
            justify-content: space-between;
            padding: 2rem;
        }

        .error-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .error-code {
            flex-direction: column;
            gap: 0;
        }

        .digit {
            font-size: 25vw;
        }

        .ghost-container {
            width: 20vw;
            height: 20vw;
        }

        .description-box {
            position: fixed;
            bottom: 7.5vw;
            left: 7.5vw;
            right: 7.5vw;
            max-width: none;
            width: auto;
        }

        .error-title {
            font-size: 1.2rem;
        }
    }
</style>
